imports:
    - { resource: config.yml }

framework:
    router:   { resource: "%kernel.root_dir%/config/routing_dev.yml" }
    profiler: { only_exceptions: false }

web_profiler:
    toolbar: false
    intercept_redirects: false

monolog:
    handlers:
        main:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
        firephp:
            type:  firephp
            level: info

assetic:
    use_controller: true

#swiftmailer:
#    delivery_address: me@example.com

# Google OAuth2 Configuration
google_apio_auth2:
    keys:
        client_id: 229411630214.apps.googleusercontent.com
        client_secret: DCCnikrtH5RNVT3_qmSVvjbz
        redirect_uri: http://local.adsense.apps.wix.com/app_dev.php/settings/redirect

    scopes:
        [ https://www.googleapis.com/auth/adsensehost ]

    preferences:
        use_objects: true

# Wix Google AdSense Configuration
wix_google_ad_sense_app:
    user:
        refresh_token: 1/p07b2UfCbppOv9avZ7PWpYaVC5LbD-dPAgUooD4qV8I